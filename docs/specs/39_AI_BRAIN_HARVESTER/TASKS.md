# 39 — AI Brain Knowledge Harvester — TASKS

**Status:** 📝 Pending user approval

> Per the SDD hard-gate in `CLAUDE.md`, the full task-by-task TDD plan (≤15 tasks, each with exact file paths, test stubs, and Verify-RED gates) is generated by the Planner via the `writing-plans` skill **only after the user approves the spec**.
>
> Provisional task outline below is informational only and will be replaced with the canonical ≤15-task plan post-approval.

## Provisional Task Outline (informational)

1. **Alembic migration — `ai_harvester_sessions`** + extend `OWNED_TABLES`
2. **Alembic migration — `consent_log`**
3. **SQLAlchemy models** — `HarvesterSession`, `ConsentRecord`
4. **Pydantic schemas** — request/response shapes per DESIGN.md API contract
5. **`consent_service.py`** — read/write `consent_log` + region-gated check
6. **`harvester_session_service.py`** — state machine, CRUD, caps, hard-delete
7. **`harvester_chat_service.py`** — probe turn + synthesis + idempotency + credit deduct
8. **Prompt module** — `prompts/harvester_probe.py`, `prompts/harvester_synthesis.py` (versioned)
9. **API router** — `/v1/brain/harvester/*` REST + SSE endpoints via `sse-starlette`
10. **Internal cron endpoint** — `/v1/internal/harvester/cleanup` (OIDC-restricted)
11. **Feature flag wiring** — `ai_brain_harvester_enabled`
12. **EDD eval suite** — golden + adversarial cases + harness + baseline run
13. **Frontend chat UI** — `apps/portal/app/(dashboard)/ai-brain/harvester/[sessionId]/page.tsx` + SSE consumer hook + Drafts tab on AI Brain page
14. **Infrastructure** — Cloud Scheduler cron + Terraform; rate-limit Memorystore keys
15. **Ops handoff** — `docs/runbooks/ai-brain-harvester.md`

Each task will follow the RED → Verify-RED → GREEN → Verify-GREEN → Commit cycle per the `tdd-workflow` skill, with file paths and test names filled in by the Planner during post-approval plan generation.
